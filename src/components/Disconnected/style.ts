import styled from 'styled-components';

const DisconnectedStyle = styled.div`
  .disconnected-content {
    margin-top: 200px;
    flex-direction: column;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .disconnected-icon {
    display: block;
    margin-top: 100px;
    margin-left: auto;
    margin-right: auto;
    margin-bottom: 20px;
    font-size: 100px;
  }
`;
export default DisconnectedStyle;
